export type LocationInfoProps = {
  name: string;
  type: string;
  rating: number;
  numReviews: number;
  costLevel: number;
  tags: string[];
};

export type Coordinates = {
  latitude: number;
  longitude: number;
};
